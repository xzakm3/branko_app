<% provide(:title, "Domov") %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <% if @user.farm %>
        <h1 id="farm-title"><%= @user.farm.name %></h1>
    <% end %>
    <%= render 'farm/farm_info' %>
  </div>
</div>